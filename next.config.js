/** @type {import('next').NextConfig} */
const nextConfig = {
  output: 'export',
  basePath: '/NamLogix-Africa',
  images: {
    unoptimized: true,
  },
  trailingSlash: true, 
}

module.exports = nextConfig